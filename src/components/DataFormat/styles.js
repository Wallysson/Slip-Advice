import styled from 'styled-components'

export const Header = styled.header`
  position: absolute;
  top: 1rem;
  right: 1rem;

  small {
    color: white;
    font-weight: 700;
    font-size: 1rem;
  }
`
